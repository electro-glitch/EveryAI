services:
  - type: web
    name: everyai
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PORT
        value: 5000
      - key: GIT_HUB_TOKEN
        sync: false
      - key: NVIDIA_KEY
        sync: false
